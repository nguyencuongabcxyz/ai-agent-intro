{
  "name": "tdd-demo",
  "version": "1.0.0",
  "description": "TDD demo — agent writes tests first, then implements",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.3.4",
    "typescript": "^5.4.0"
  }
}
